---
id: G-001
title: 实现 Phase 2 认知架构最小可用集
status: in-progress
priority: P0
owner: "@Architect"
created: 2026-04-25
updated: 2026-04-25
related_decisions: [D-002, D-003]
related_tickets: [T-001, T-002, T-003, T-004]
related_prs: []
---

# Goal: 实现 Phase 2 认知架构最小可用集

## One-sentence statement

让本仓库的 agent 从"事件驱动的脚本调度器"升级为"具有持续目标 + 反思能力 + 工作集装配"的智能体,使 `docs/agent-cognitive-architecture.md` §6 / §7 / §9 三个核心机制可运行。

## Acceptance Criteria

- [ ] **Goal Stack 可读写**:存在 `scripts/goal_stack.py`,提供 `list / show / advance / set-status` 四个子命令,通过 CLI 即可对 `goals/G-NNN.md` 文件进行增量推进与状态切换。
- [ ] **反思闭环自动化**:`scripts/run_skill.py` 在每次 skill 调用结束后自动追加一条 `reflections/R-NNN.md`,内容包含四问回顾(目标是否达成 / 用了哪些记忆 / 缺什么 / 下次怎么改)。
- [ ] **Working Set 装配独立成步**:`scripts/run_skill.py` 调用一个新模块 `scripts/assemble_context.py` 来组装 ContextPack,而不是直接把整个 `MEMORY.md` 塞进 prompt。装配规则按 `docs/agent-cognitive-architecture.md` §7 的 Phase 2 朴素方案实现。
- [ ] **Goal-driven 触发**:在 `.github/workflows/dispatcher.yml` 加一条独立的 `schedule` 触发(每天一次),让 dispatcher 读 Goal Stack,选出"今日最该推进的 goal",生成对应 issue 评论或更新 goal 的 `Last advanced`。
- [ ] **Skill 协议升级**:每个 `.agents/skills/<id>/skill.md` 新增 `cognitive_mode` 与 `collab_tier` 两个 front-matter 字段,`validate-skills.yml` 强制校验。
- [ ] **互联图同步**:`AGENTS.md` 与 `MEMORY.md` 的 Interconnection Map 包含 `goals/`、`reflections/`、`scripts/goal_stack.py`、`scripts/assemble_context.py` 的级联规则。
- [ ] **反思笔记落档**:G-001 关闭时,必须在 `reflections/` 下追加一条 `R-NNN.md` 引用本 goal id。

## Subtasks

通过 ticket 拆解推进(在 `AGENT-COLLAB.md` 中流转)。当前已开:

- [x] **T-001** —— Goal Stack 最小骨架(`scripts/goal_stack.py` + `goal-driven.yml` + 20 unittest)。**已完成,PR #12 merged。**
- [ ] **T-002** —— 反思闭环自动化(`scripts/append_reflection.py` + `run_skill.py` hook)。**实施完成,PR open,待 @Architect review。**
- [x] T-003(完成)—— Working Set 装配(`scripts/assemble_context.py`)。**实施完成,PR open,待 @Architect review。**
- [ ] T-004(待开)—— Skill 协议升级(`cognitive_mode` + `collab_tier`)。

## Blockers

(none)

## Last advanced

- 2026-04-29 by @Copilot: T-003 完成:实现 assemble_context.py 四层 ContextPack 装配、run_skill.py 集成（assemble_context 优先 + memory_excerpt fallback）、16 个 unittest 全通过,reflections/R-003.md 已落档。
- 2026-04-26 by @Copilot: T-002 完成:实现 append_reflection.py、run_skill.py 双模式 reflect hook、18 个 unittest 全通过,reflections/R-002.md 已落档。
- 2026-04-25 by @Copilot: T-001 完成:实现 goal_stack.py (list/show/advance/set-status)、goal-driven.yml、20 个 unittest 全通过,reflections/R-001.md 已落档。

## Lessons learned

- Phase 1 的 `model_tier` 字段只区分模型贵贱,没区分思维方式。Phase 2 必须把"思维模式"独立出来(`cognitive_mode`),否则 `dispatcher.yml` 无法做对路由。
- AGENT-COLLAB.md 的 `Files in scope` 字段是防止 @Copilot 越界的关键护栏——架构文档与协议文件必须列入"禁改"。
- 当本地仓库与远程并发改动时,**未 push 的 Phase 工作可能被 pull 覆盖**。后续每次架构改动应:先 commit 立刻 push,不要积累多次未推送的改动;否则丢失重建成本高(本次重建即教训)。
