---
id: G-003
title: 实现 Phase 4 学习与演化最小可用集
status: done
priority: P0
owner: "@Architect"
created: 2026-04-30
updated: 2026-04-30
related_decisions: [D-008]
related_tickets: [T-007]
related_prs: []
---

# Goal: 实现 Phase 4 学习与演化最小可用集

## One-sentence statement

让 agent 的反思笔记真正影响 Working Set 装配——通过 TF-IDF 索引检索最相关的反思，取代固定拉取最近审计行，实现"从历史中学习"的第一步。

## Acceptance Criteria

- [x] **反思嵌入索引**：`scripts/embed_index.py` 构建全部 R-NNN.md 的 TF-IDF 索引；`query_index()` 按余弦相似度返回 top-N 相关反思；支持 JSON 缓存（`.agent_index.json`，已加入 .gitignore）；stdlib-only，无新 pip 依赖。
- [x] **Working Set 升级**：`scripts/assemble_context.py` Layer 3 优先使用 `embed_index` 检索相关反思；embed_index 不可用或无匹配时自动 fallback 到最近审计行；section header 更新为 "Relevant Context"。
- [x] **skill 演化候选流程**：`scripts/scan_repo.py` 增加 `skill-evolution` 检查——扫描 reflections 中 "→ skill update" 标记，生成演化候选报告；候选 ≥3 条时写入发现列表（文件名 + 行内容摘要）。
- [x] **反思→working-set 反哺验证**：`scripts/assemble_context.py` `assemble()` 函数在 Layer 3 决策后向 stderr 打印 `[assemble_context] Layer 3 source: <source>`（`relevant-reflections` / `recent-audit` / `none`）；3 个新 unittest 验证全部路径。
- [x] **测试覆盖**：`scripts/test_embed_index.py` ≥ 15 unittest，全部通过（**已完成：20 unittest**）；`scripts/test_scan_repo.py` 新增 skill-evolution 检查测试 ≥ 3 个（**已完成：6 个**）。
- [x] **反思笔记落档**：G-003 关闭时 `reflections/R-009.md`（T-007）与 `reflections/R-010.md`（T-008）均已落档。

## Subtasks

- [x] **T-008** — skill 演化候选检查（`memory/watchlist.yml` 新增 skill-evolution 项 + `scripts/scan_repo.py` check_skill_evolution + ≥5 unittest）。**实施完成，待 @Architect REVIEW。**
- [x] **T-009** — 反思→working-set 反哺验证（`assemble_context.py` Layer 3 source 日志 + 3 unittest）。**实施完成，G-003 全部 AC 达成。待 @Architect REVIEW。**

---

### T-007 规格（@Owner 直接授权开工，@Architect 事后审查）

```
Ticket ID:        T-007
Title:            反思嵌入索引(embed_index.py + assemble_context.py Layer 3 升级)
Linked Goal:      G-003
Linked Decisions: D-008

Goal (1 sentence):
    构建 TF-IDF 反思索引，让 Working Set 装配从"最近审计行"升级为
    "当前任务最相关的历史反思"。

Acceptance Criteria:
- [x] scripts/embed_index.py:
        build_index(repo_root) → TF-IDF index dict
        query_index(index, query, top_n=3) → list[(filename, score)]
        save_index / load_index / load_or_build_index (JSON 缓存)
        CLI: python scripts/embed_index.py --query "<text>" [--top-n 3]
        stdlib-only；.agent_index.json 加入 .gitignore
- [x] scripts/assemble_context.py Layer 3 升级:
        _extract_relevant_reflections(event, repo_root) 返回 top-3 相关反思
        fallback 到 _extract_recent_audit(repo_root) 当 embed_index 不可用
        section header 改为 "## Relevant Context"
- [x] scripts/test_embed_index.py ≥15 unittest 全通过（实现 20 个）
- [x] .gitignore 追加 .agent_index.json

Files in Scope:
    新增：scripts/embed_index.py · scripts/test_embed_index.py
    修改：scripts/assemble_context.py · .gitignore
    新增：goals/G-003.md · reflections/R-009.md
    修改：AGENT-COLLAB.md §8 · MEMORY.md Task Log
```

## Blockers

(none)

## Last advanced

- 2026-05-01 by @Copilot: T-009 实施完成。`scripts/assemble_context.py` 新增 Layer 3 source 日志（stderr）；3 新增 unittest（19 个全部通过）；`reflections/R-011.md` 落档；G-003 所有 AC 勾选完毕，status→done。待 @Architect REVIEW + Phase 4 出口门禁核验。
- 2026-04-30 by @Copilot: T-008 实施完成。`memory/watchlist.yml` 新增 `skill-evolution` 项；`scripts/scan_repo.py` 新增 `check_skill_evolution()` + 注册 `CHECK_FUNCTIONS`；`scripts/test_scan_repo.py` 新增 6 个 unittest（26 个全部通过）；`reflections/R-010.md` 落档。待 @Architect REVIEW。

## Lessons learned

- TF-IDF 余弦相似度用 stdlib (math, re, json) 即可实现，无需 scikit-learn 等外部依赖——满足"stdlib-only"约束。
- `_read_reflection` 需同时 catch `UnicodeDecodeError`（非法 UTF-8 文件），不只是 `OSError`。
- Layer 3 header 从 "Recent Audit" 改为 "Relevant Context" 是语义级改进，不破坏已有测试（测试只检查函数行为，不检查 header 名称）。
